# My Jobs
### Flow the instructions

- Install node modules
    `yarn`
    or
    `yarn install`
    or
    `npm install`

- Install pods
    `cd ios && pod install`

- Start project
    `yarn ios`
    or
    `react-native run-ios`

